# Dotfiles

This repo contains my personal dotfiles managed with symlinks.

## Setup

```sh
./install.sh
```

## Notes

- Machine-specific or secret data goes in `local/` (ignored by git).
- Add new files by placing them in this repo and adding them to the mapping list in `install.sh`.
